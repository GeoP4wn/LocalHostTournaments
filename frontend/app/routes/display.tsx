export const handle = {
  sidebarLinks: [
    { label: "< Back", to: "/" },
  ],
};

export default function Display()  {
    return(
        <div>
            <h1>Display the current Tournament</h1>
        </div>
    );
}