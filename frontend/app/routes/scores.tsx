export const handle = {
  sidebarLinks: [
    { label: "< Back", to: "/" },
  ],
};

export default function Scores() {
    return(
        <div>
            <h1>Input the scores during a Tournament</h1>
        </div>
    );
}